{
  "name": "@sgt/rules-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Motor de reglas laborales: horas diurnas/nocturnas/dominicales y valor de recargos (TS puro, sin E/S)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^3.2.7"
  }
}
