{
  "name": "@sgt/shared-types",
  "version": "0.1.0",
  "private": true,
  "description": "Enums, DTOs y parámetros por defecto compartidos entre la API y la app de escritorio",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
